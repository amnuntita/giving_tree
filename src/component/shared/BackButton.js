import React from 'react';
import {Button} from 'reactstrap';

const Back = () => {
    return(
        <div class="d-flex align-items-start">
        <Button style={{margin:10}}>Back Button</Button>
      </div>
    );
}

export default Back;